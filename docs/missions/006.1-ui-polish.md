Read AGENTS.md and follow it strictly.

Mission: Implement UI polish improvements for Alerts page (Mission 006.1).

Scope:
1) Improve chart sharpness:
   - Do NOT change layout.
   - Assume backend charts may now be higher resolution.
   - Ensure <img> is displayed at natural aspect ratio (no stretching).
   - Use responsive width with crisp rendering.

2) Alert card layout improvements:
   - Move LINK button ABOVE the chart (top-right of card).
   - Restructure text above the chart into clear hierarchy:
     a) Header row:
        - Symbol (large)
        - alert_type as a chip
        - LINK button aligned right
     b) Meta row (single line, muted):
        - Triggered date/time
        - Price
     c) Optional message row (smaller, secondary text)

   - Reduce paragraph-like stacked text.
   - Prefer compact rows with separators (•)
   - Add dollar sign before the price number.

3) Tags styling and placement:
   - Move tags BELOW the chart and OUTSIDE the white card.
   - Style tags as pastel chips:
     - Slightly larger than default MUI Chip
     - Soft pastel background colors
     - Rounded (pill)
     - Subtle but eye-catching
   - Tags row should feel visually connected to the card but not inside it.

Do NOT:
- Change backend APIs.
- Add new features.
- Rework navigation or polling logic.

Acceptance:
- Alerts page looks visually calmer and more intentional.
- Chart appears sharper than before.
- Tags feel like interpretation, not raw data.
- LINK action is clearly visible before the chart.

Return:
- List of files changed
- Before/after screenshots if possible
- suggestions for further UI improvment